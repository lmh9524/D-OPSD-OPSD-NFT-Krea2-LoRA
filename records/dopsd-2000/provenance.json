{
  "git_sha": "68a6a7f80b22e6f4e9ed47b8f96328ac8c2b8db6",
  "git_branch": "lihaoran/opsd/dopsd-krea2-ref2img",
  "git_dirty": true,
  "python": "3.12.14",
  "platform": "Linux-7.0.0-1011-gcp-x86_64-with-glibc2.39",
  "torch": "2.14.0+cu130",
  "cuda": "13.0",
  "gpu": "NVIDIA RTX PRO 6000 Blackwell Server Edition",
  "diffusers": "0.39.0",
  "git_diff": "diff --git a/dflow/config/train.py b/dflow/config/train.py\nindex 1eb4d43..7f88508 100644\n--- a/dflow/config/train.py\n+++ b/dflow/config/train.py\n@@ -65,6 +65,12 @@ class CheckpointConfig:\n     preview_max_area: int = 384 * 672\n     #: Index into the dataset to render. Fixed, so previews across checkpoints are comparable.\n     preview_index: int = 0\n+    #: Directory of a HELD-OUT ref2img dataset (train.jsonl + images/) to preview instead of the\n+    #: training set \u2014 real test cases the model never trained on. ``None`` falls back to\n+    #: ``preview_index`` into the training dataset.\n+    preview_dataset: str | None = None\n+    #: How many cases from ``preview_dataset`` to render at each checkpoint.\n+    preview_count: int = 5\n \n \n @dataclass(kw_only=True, slots=True)\ndiff --git a/experiments/dopsd_krea2_ref2img_lora.py b/experiments/dopsd_krea2_ref2img_lora.py\nindex e0dfd37..883d1af 100644\n--- a/experiments/dopsd_krea2_ref2img_lora.py\n+++ b/experiments/dopsd_krea2_ref2img_lora.py\n@@ -202,6 +202,15 @@ def _rollout_losses(cfg, model, batch, *, vae, text, family, generator, seen_sha\n                     teacher_out, target_len, target_offset=teacher_seq.target_offset\n                 )\n \n+            # Restore the student adapter BEFORE the loss/yield. Under activation checkpointing the\n+            # student forward is recomputed during backward, and it must recompute with \"default\"\n+            # (the trainable student) active \u2014 not \"teacher\". fit_distill backwards each yield while\n+            # this generator is suspended here, so if \"teacher\" were still active the recompute would\n+            # apply the frozen teacher adapter and the student would receive no gradient (grad_norm\n+            # == 0, nothing learns). The frozen-base path already restores via disable_adapter()'s\n+            # context, but the EMA path leaves \"teacher\" active, so reset it explicitly.\n+            model.set_adapter(student)\n+\n             if d.loss_space == \"x0\":\n                 # x_t = (1 - sigma) x0 + sigma * noise  =>  x0 = x_t - sigma * v\n                 prediction = state - sigma * v_student\n@@ -272,6 +281,7 @@ def make_preview_fn(cfg, *, model, dataset, vae, text, family, generator, device\n     import datetime\n     import json\n     import pathlib\n+    from dataclasses import replace\n \n     import numpy as np\n     from diffusers import FlowMatchEulerDiscreteScheduler\n@@ -285,106 +295,133 @@ def make_preview_fn(cfg, *, model, dataset, vae, text, family, generator, device\n     steps, guidance = int(defaults[\"steps\"]), float(defaults[\"guidance\"])\n     demo_root = pathlib.Path(__file__).resolve().parent.parent / \"demo\"\n \n-    sample = dataset[cfg.checkpoint.preview_index]\n-    ref_pixels = [r.unsqueeze(0).to(device) for r in sample[\"references\"]]\n-    prompt = sample[\"prompt\"]\n-\n-    with torch.no_grad():\n-        reference_latents = [vae.encode(r, mode=\"mode\").float() for r in ref_pixels]\n-        grid = vae.encode(sample[\"target\"].unsqueeze(0).to(device), mode=\"mode\")\n-        conditioning = text.encode_grounded(prompt, ref_pixels) if ground else text.encode([prompt])\n-    grid_shape = tuple(grid.shape)\n-\n-    # Fixed noise so the student and the baseline start from the same point (a fair comparison).\n-    preview_gen = torch.Generator(device=device).manual_seed(20260911)\n-    preview_noise = torch.randn(grid_shape, generator=preview_gen, device=device, dtype=torch.float32)\n     stepper = FlowMatchEulerDiscreteScheduler.from_pretrained(weights, subfolder=\"scheduler\")\n-    text_ids = build_krea2_text_ids(conditioning.embeds.shape[1], device=device)\n-\n-    def _render(m):\n-        sequence = build_krea2_sequence(\n-            target_latents=preview_noise.clone(),\n-            reference_latents=reference_latents,\n-            registration=registration,\n-            t_scale=t_scale,\n-        )\n-        stepper.set_timesteps(\n-            sigmas=np.linspace(1.0, 1.0 / steps, steps).tolist(),\n+\n+    # Preview on a HELD-OUT try-on test set if given (real cases, never trained on); else one\n+    # training sample. Reuse the training dataset's geometry so the test cases are processed the\n+    # same way, only the root changes.\n+    if cfg.checkpoint.preview_dataset:\n+        preview_ds = Ref2ImgDataset(replace(cfg.dataset, root=cfg.checkpoint.preview_dataset))\n+        count = min(cfg.checkpoint.preview_count, len(preview_ds))\n+        samples = [preview_ds[i] for i in range(count)]\n+        logger.info(f\"preview: {count} held-out test cases from {cfg.checkpoint.preview_dataset}\")\n+    else:\n+        samples = [dataset[cfg.checkpoint.preview_index]]\n+\n+    def _to_pil(t):\n+        arr = ((t.squeeze(0).clamp(-1, 1) + 1.0) * 127.5).byte().permute(1, 2, 0).cpu().numpy()\n+        return Image.fromarray(arr)\n+\n+    def _prepare(sample, seed):\n+        ref_pixels = [r.unsqueeze(0).to(device) for r in sample[\"references\"]]\n+        with torch.no_grad():\n+            reference_latents = [vae.encode(r, mode=\"mode\").float() for r in ref_pixels]\n+            grid = vae.encode(sample[\"target\"].unsqueeze(0).to(device), mode=\"mode\")\n+            conditioning = (\n+                text.encode_grounded(sample[\"prompt\"], ref_pixels)\n+                if ground\n+                else text.encode([sample[\"prompt\"]])\n+            )\n+        grid_shape = tuple(grid.shape)\n+        # Fixed noise per case so student and baseline start identically (a fair comparison).\n+        noise = torch.randn(\n+            grid_shape,\n+            generator=torch.Generator(device=device).manual_seed(seed),\n             device=device,\n-            mu=family.noise_shift_mu(image_tokens=sequence.target_len),\n-        )\n-        stepper.set_begin_index(0)\n-        latents = denoise(\n-            model=m, family=family, sequence=sequence,\n-            text_embeds=conditioning.embeds.to(m.dtype), text_ids=text_ids,\n-            text_mask=conditioning.mask, timesteps=stepper.timesteps, scheduler=stepper,\n-            num_train_timesteps=stepper.config.num_train_timesteps,\n-            guidance=guidance, negative_embeds=None,\n+            dtype=torch.float32,\n         )\n-        return vae.decode_to_pil(latents, height=grid_shape[-2], width=grid_shape[-1])\n+        text_ids = build_krea2_text_ids(conditioning.embeds.shape[1], device=device)\n+\n+        def render(m):\n+            sequence = build_krea2_sequence(\n+                target_latents=noise.clone(),\n+                reference_latents=reference_latents,\n+                registration=registration,\n+                t_scale=t_scale,\n+            )\n+            stepper.set_timesteps(\n+                sigmas=np.linspace(1.0, 1.0 / steps, steps).tolist(),\n+                device=device,\n+                mu=family.noise_shift_mu(image_tokens=sequence.target_len),\n+            )\n+            stepper.set_begin_index(0)\n+            latents = denoise(\n+                model=m, family=family, sequence=sequence,\n+                text_embeds=conditioning.embeds.to(m.dtype), text_ids=text_ids,\n+                text_mask=conditioning.mask, timesteps=stepper.timesteps, scheduler=stepper,\n+                num_train_timesteps=stepper.config.num_train_timesteps,\n+                guidance=guidance, negative_embeds=None,\n+            )\n+            return vae.decode_to_pil(latents, height=grid_shape[-2], width=grid_shape[-1])\n+\n+        return {\n+            \"render\": render,\n+            \"refs\": [_to_pil(r) for r in ref_pixels],\n+            \"prompt\": sample[\"prompt\"],\n+            \"grid\": grid_shape,\n+            \"n_refs\": len(ref_pixels),\n+        }\n \n-    # Baseline = the original phase-1 LoRA, rendered now while \"default\" still holds it (pre-training).\n+    # Prepare each case and cache its phase-1 baseline now, while \"default\" still holds phase-1.\n     was_training = model.training\n     model.eval()\n     model.set_adapter(student)\n+    states = []\n     with torch.no_grad():\n-        baseline = _render(model)\n+        for index, sample in enumerate(samples):\n+            state = _prepare(sample, 20260911 + index)\n+            state[\"baseline\"] = state[\"render\"](model)\n+            states.append(state)\n     model.train(was_training)\n-    logger.info(\n-        f\"preview: cached phase-1 baseline {baseline.width}x{baseline.height} | {steps} steps, \"\n-        f\"cfg {guidance}, sample {cfg.checkpoint.preview_index}, {len(ref_pixels)} refs\"\n-    )\n-\n-    def _to_pil(t):\n-        arr = ((t.squeeze(0).clamp(-1, 1) + 1.0) * 127.5).byte().permute(1, 2, 0).cpu().numpy()\n-        return Image.fromarray(arr)\n-\n-    ref_images = [_to_pil(r) for r in ref_pixels]\n+    logger.info(f\"preview: cached {len(states)} phase-1 baseline(s) | {steps} steps, cfg {guidance}\")\n \n     def preview_fn(model, step, path):\n         model.eval()\n         model.set_adapter(student)\n-        with torch.no_grad():\n-            current = _render(model)\n-        gap, bar = 8, 30\n-        panel = Image.new(\n-            \"RGB\",\n-            (current.width + gap + baseline.width, bar + max(current.height, baseline.height)),\n-            \"white\",\n-        )\n-        draw = ImageDraw.Draw(panel)\n-        draw.text((6, 9), f\"D-OPSD student  \u00b7  step {step}\", fill=\"black\")\n-        draw.text((current.width + gap + 6, 9), \"original phase-1 LoRA\", fill=\"black\")\n-        panel.paste(current, (0, bar))\n-        panel.paste(baseline, (current.width + gap, bar))\n-\n         now = datetime.datetime.now()\n-        out = demo_root / now.strftime(\"%Y-%m-%d\") / f\"{now.strftime('%H%M%S')}-{step:06d}\"\n-        (out / \"refs\").mkdir(parents=True, exist_ok=True)\n-        panel.save(out / \"panel.png\")\n-        for i, image in enumerate(ref_images, start=1):\n-            image.save(out / \"refs\" / f\"R{i}.png\")\n-        (out / \"prompt.txt\").write_text(prompt + \"\\n\", encoding=\"utf-8\")\n-        (out / \"meta.json\").write_text(\n-            json.dumps(\n-                {\n-                    \"step\": step,\n-                    \"preview_index\": cfg.checkpoint.preview_index,\n-                    \"prompt\": prompt,\n-                    \"n_refs\": len(ref_images),\n-                    \"steps\": steps,\n-                    \"guidance\": guidance,\n-                    \"family\": cfg.backbone.model.family,\n-                    \"latent_grid\": list(grid_shape),\n-                    \"panels\": [\"D-OPSD student (current)\", \"original phase-1 LoRA (baseline)\"],\n-                },\n-                indent=2,\n-                ensure_ascii=False,\n+        base = demo_root / now.strftime(\"%Y-%m-%d\") / f\"{now.strftime('%H%M%S')}-{step:06d}\"\n+        for index, state in enumerate(states):\n+            with torch.no_grad():\n+                current = state[\"render\"](model)\n+            baseline = state[\"baseline\"]\n+            gap, bar = 8, 30\n+            panel = Image.new(\n+                \"RGB\",\n+                (current.width + gap + baseline.width, bar + max(current.height, baseline.height)),\n+                \"white\",\n             )\n-            + \"\\n\",\n-            encoding=\"utf-8\",\n-        )\n-        logger.info(f\"preview (student | phase-1) step {step} -> {out}\")\n+            draw = ImageDraw.Draw(panel)\n+            draw.text((6, 9), f\"D-OPSD student  \u00b7  step {step}\", fill=\"black\")\n+            draw.text((current.width + gap + 6, 9), \"original phase-1 LoRA\", fill=\"black\")\n+            panel.paste(current, (0, bar))\n+            panel.paste(baseline, (current.width + gap, bar))\n+\n+            out = base / f\"case_{index}\"\n+            (out / \"refs\").mkdir(parents=True, exist_ok=True)\n+            panel.save(out / \"panel.png\")\n+            for j, image in enumerate(state[\"refs\"], start=1):\n+                image.save(out / \"refs\" / f\"R{j}.png\")\n+            (out / \"prompt.txt\").write_text(state[\"prompt\"] + \"\\n\", encoding=\"utf-8\")\n+            (out / \"meta.json\").write_text(\n+                json.dumps(\n+                    {\n+                        \"step\": step,\n+                        \"case\": index,\n+                        \"prompt\": state[\"prompt\"],\n+                        \"n_refs\": state[\"n_refs\"],\n+                        \"steps\": steps,\n+                        \"guidance\": guidance,\n+                        \"family\": cfg.backbone.model.family,\n+                        \"latent_grid\": list(state[\"grid\"]),\n+                        \"panels\": [\"D-OPSD student (current)\", \"original phase-1 LoRA (baseline)\"],\n+                    },\n+                    indent=2,\n+                    ensure_ascii=False,\n+                )\n+                + \"\\n\",\n+                encoding=\"utf-8\",\n+            )\n+        logger.info(f\"preview: {len(states)} case(s) (student | phase-1) step {step} -> {base}\")\n \n     return preview_fn"
}