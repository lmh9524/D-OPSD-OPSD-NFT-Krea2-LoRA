{
  "git_sha": "5ebcb82a82bfd44aea79e7d3dfd759cedcb77f53",
  "git_branch": "lihaoran/opsd/dopsd-krea2-ref2img",
  "git_dirty": true,
  "python": "3.12.14",
  "platform": "Linux-7.0.0-1011-gcp-x86_64-with-glibc2.39",
  "torch": "2.14.0+cu130",
  "cuda": "13.0",
  "gpu": "NVIDIA RTX PRO 6000 Blackwell Server Edition",
  "diffusers": "0.39.0",
  "git_diff": "diff --git a/experiments/krea2_ref2img_opsd_nft.py b/experiments/krea2_ref2img_opsd_nft.py\nindex 0bfdc91..22726cd 100644\n--- a/experiments/krea2_ref2img_opsd_nft.py\n+++ b/experiments/krea2_ref2img_opsd_nft.py\n@@ -243,8 +243,14 @@ def make_closures(cfg, *, vae, text, family, generator, logger):\n             model.set_adapter(old)\n             for k in range(K):\n                 timestep = sigmas[k].expand(group_size).to(model.dtype)\n-                tokens = sequence.replace_target(state).tokens\n-                tokens = tokens.expand(group_size, -1, -1).to(model.dtype)\n+                # Refs are shared across the group (the sequence is batch-1) but the target is\n+                # per-member (`state` is (G, target_len, C)). replace_target can't cat a batch-1 ref\n+                # span with a batch-G target, so build the batched sequence explicitly: expand the\n+                # ref spans to G and concatenate the per-member noised targets.\n+                off = sequence.target_offset\n+                pre = sequence.tokens[:, :off].expand(group_size, -1, -1)\n+                post = sequence.tokens[:, off + target_len :].expand(group_size, -1, -1)\n+                tokens = torch.cat([pre, state, post], dim=1).to(model.dtype)\n                 velocity = family.take_target_span(\n                     model(\n                         **family.prepare_inputs(\n@@ -356,9 +362,12 @@ def make_closures(cfg, *, vae, text, family, generator, logger):\n         with torch.no_grad():\n             model.set_adapter(old)\n             old_prediction = forward(tokens).float()\n-            # Reference: the base model with every adapter disabled.\n-            with model.disable_adapter():\n-                ref_prediction = forward(tokens).float()\n+            # Reference: the base model with LoRA disabled. Krea2's diffusers model exposes\n+            # disable_adapters()/enable_adapters() (methods), not peft's disable_adapter() context\n+            # manager, so toggle explicitly and re-enable before restoring the student adapter.\n+            model.disable_adapters()\n+            ref_prediction = forward(tokens).float()\n+            model.enable_adapters()\n         # Restore the trainable adapter before the loss/backward: under activation checkpointing the\n         # grad-carrying forward is recomputed in backward and must recompute with \"default\" active,\n         # or the student receives no gradient (the D-OPSD experiment documents this trap).\ndiff --git a/scripts/train_opsd_nft_krea2.sh b/scripts/train_opsd_nft_krea2.sh\nindex 4adcf66..b849174 100755\n--- a/scripts/train_opsd_nft_krea2.sh\n+++ b/scripts/train_opsd_nft_krea2.sh\n@@ -51,7 +51,6 @@ python3 experiments/krea2_ref2img_opsd_nft.py \\\n     --nft.ref-kl-coef \"${REF_KL_COEF:-1e-4}\" \\\n     --nft.num-train-timesteps \"${NUM_TRAIN_TIMESTEPS:-8}\" \\\n     --nft.old-policy-decay \"${OLD_POLICY_DECAY:-0.0}\" \\\n-    --nft.reward.reference-fidelity.enabled True \\\n     --training.steps \"${STEPS:-500}\" \\\n     --training.grad-accum-steps \"${GRAD_ACCUM:-4}\" \\\n     --checkpoint.interval \"${INTERVAL:-100}\" \\"
}